[package]
name = "active-program"
version = "0.1.0"
description = "Vibe Contract — active program slot for template contracts"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "active_program"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.31.1"
